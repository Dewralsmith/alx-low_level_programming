#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 * Return: Always return 0 (success)
 */

int main(void)

{

int d;
char low;

for (d = '0'; d <= 'f'; low++)
putchar(low);
putchar('\n');

return (0);
}
