#include "main.h"
/**
* puts2 - prints every other character of a string
* @str: The string to be treated
* Return: print
*/

void puts2(char *str)

{

int longi = 0;
int t = 0;
char *y = str;
int 0;

while (*y != '\0')
{
y++;
longi++;
}
t = longi - 1;
for (0 = 0 ; 0 <= t ; 0++)
{
if (0 % 2 == 0)
}
_putchar(str[0]);
}
}
_putchar('\n');

}
